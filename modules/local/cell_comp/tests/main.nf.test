nextflow_process {

    name "Test CELL_COMP"
    script "../main.nf"
    process "CELL_COMP"
    tag "modules"
    tag "modules_local"
    tag "cell_comp"

    test("stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = file("stub_rg.rds")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
