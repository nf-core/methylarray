nextflow_process {

    name "Test RAW_INTENSITIES_QC"
    script "../main.nf"
    process "RAW_INTENSITIES_QC"
    tag "modules"
    tag "modules_local"
    tag "raw_intensities_qc"

    test("stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = file("stub_rg.rds")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
