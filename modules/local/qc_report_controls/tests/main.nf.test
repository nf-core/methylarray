nextflow_process {

    name "Test QC_REPORT_CONTROLS"
    script "../main.nf"
    process "QC_REPORT_CONTROLS"
    tag "modules"
    tag "modules_local"
    tag "qc_report_controls"

    test("stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = file("stub_rg.rds")
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
