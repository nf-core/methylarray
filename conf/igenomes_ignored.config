/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file — iGenomes ignored
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    This pipeline does not use iGenomes reference files. This config satisfies
    the nf-core lint requirement while signalling that iGenomes are not needed.
----------------------------------------------------------------------------------------
*/

params {
    igenomes_ignore = true
}
